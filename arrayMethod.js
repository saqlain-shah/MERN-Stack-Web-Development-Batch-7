// in this we will cover array method
//1:isArray,length,push.pop,shift ,unshift,foreach,
// Object:method

// ****************************************push method************************************************

let arr =['ali','aslam','akbar','yousf'];
console.log(arr);
let newArr=arr.push("noor");// it will add new name to at the end of array
console.log(newArr);